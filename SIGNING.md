# LifeOS signing identity

The V0.1 prototype APK is signed with the dedicated LifeOS debug identity supplied separately. Keep that keystore private and unchanged: Android updates must use the same signer. Do not commit it to a public repository.

For a production release, choose and securely archive a permanent release identity before distributing the application beyond the owner's test device.
